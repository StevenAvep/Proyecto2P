<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyetoGrupo9"
        tools:targetApi="31">

        <!-- Actividad de Login como principal -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad principal del menú -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name" />

        <!-- Actividades adicionales -->
        <activity android:name=".Etapa" />
        <activity android:name=".Residencia" />
        <activity android:name=".Contacto" />
        <activity android:name=".Evento" />
        <activity android:name=".VehiculoResidente" />
        <activity android:name=".VehiculoVisitante" />
        <activity android:name=".ListadoVehiculos" />
        <activity android:name=".VisitanteEvento" />

    </application>

</manifest>